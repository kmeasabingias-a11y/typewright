# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual env / uv
.venv/

# Test / tooling caches
.pytest_cache/
.hypothesis/
.ruff_cache/
.mypy_cache/

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/

# Local agent tooling — never commit
.claude/

# Internal planning doc (private strategy notes) — keep local only
TypeWright_Project_Brief.pdf

# GitHub App private key — never commit
*.pem

# Phase 8 web-demo run store (local SQLite)
runs.db
runs.db-wal
runs.db-shm

# Eval harness: target libraries are installed locally (see eval/harness/README.md)
eval/harness/libs/
